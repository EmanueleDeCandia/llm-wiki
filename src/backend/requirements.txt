# LLM Wiki — backend sidecar (Python 3.11+)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
PyYAML>=6.0
httpx>=0.27

# Ingestione multimodale (lightweight, senza GPU)
pypdf>=4.2
Pillow>=10.2
polars>=1.0

# Test
pytest>=8.0

# --- Opzionali (attivi solo se installati, mai richiesti a runtime) ---------
# docling>=2.10           # PDF ad alta fedeltà: tabelle strutturate, gerarchia,
#                         # formule (LLW_PDF_ENGINE=auto|docling, LLW_PDF_FORMULAS=1)
# marker-pdf>=1.0         # alternativa a docling (LLW_PDF_ENGINE=marker)
# python-docx>=1.1        # DOCX avanzato (built-in usa OOXML diretto)
# jupyter-client>=8.0     # kernel Jupyter persistente (LLW_KERNEL=jupyter)
# matplotlib / seaborn    # installati NEL VENV DEL SANDBOX per gli script generati
